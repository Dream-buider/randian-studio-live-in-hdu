# LIVE IN HDU GOAI Agent - 环境变量示例
# 复制此文件为 .env.local 并填写真实值
# 注意：.env.local 已包含在 .gitignore，不会进入公开仓库

# TokenDance（可选，无密钥时系统仍可运行本地预设和知识库）
TOKENDANCE_API_KEY=

# 数据库（可选，默认使用 SQLite）
DATABASE_URL=

# 服务端端口（默认 3210）
PORT=3210
